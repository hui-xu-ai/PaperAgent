## 双源架构经验（2026-08-18）
结构用本地 PyMuPDF 行级 + stitch（标题合并/图注分组/顶格续行/引文编号完整句）；
文本主体用 MinerU 高精度 full.md（LaTeX 完整），build_mineru_doc 段落级替换（4-gram 覆盖率归属，段落守恒）。

## 校验分层经验（2026-08-18）
语法层 latex_check（命令白名单/美元符/花括号/env 配对，自动定位错误源）；
语义层 anomaly_detect（� 检测 + KNOWN_FIXES 已知修复库 + 清单供 AI 子代理判断）。
AI 只能通过 LaTeX 语法定位（用户指正），语义判断交 AI。

## 翻译/总结提示词规范（专业默认，2026-08-18，待 M5 实现后生效）
- 总结：背景/方法/结果/结论/创新点/局限 六段式；客观、不臆造数字。
- 翻译：忠实原文、保留 LaTeX 公式与 <sup>；中英对照输出（<details> 折叠英文）；
  术语统一（可在 prompts/translate.json 自定义 glossary）。

## 运行纪律(用户需求6)（2026-08-18T16:36:11+00:00）

反复调试同一bug导致出现多个新bug时，必须停止并主动向用户汇报当前状态与方案，禁止擅作主张继续尝试；单次编辑错误应即刻修复并保持透明。


## U5翻译总结上下文与效率决策（2026-08-18T17:37:53+00:00）

1) 主进程集成路径run_m5=两次独立AI调用(summary+translate各带完整prompt),输入上下文会重复; 2) Harness子代理路径=一个上下文持有全文一次,总结+翻译同窗口产出,输入token更低——用户优先用子代理,主进程省上下文。3) 拼接=按段落idx写回text_zh+ai_summary,render读取text_zh(中文上英文下不折叠)+ai_summary+journal渲染。4) 尽量一次性给全文让AI完整理解论文,避免分批重复。

