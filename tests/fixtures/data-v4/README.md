# 黄金数据夹具 data-v4

冻结时间：2026-09-19T19:10:27 · data_format=4 · layout=v1

用途：`tools/check_data_compat.py --fixtures-only` 与 `backend/tests/test_version_contract.py` 用它验证「新代码能否打开并迁移旧格式数据」。**不要手工改这里的内容**——它是历史格式的快照。

> 已由 `tools/sanitize_fixtures.py` **脱敏**：API Key / 本机绝对路径 / 用户生成文本（对话、笔记、标题）一律替换为占位值；schema、表结构与行数不变。
