#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""重写 AGENT_FEEDBACK V11 为综合版（覆盖 08-26 修复轮 + 08-27 卡帕西知识库会话）"""
from pathlib import Path

V11 = '''

---

# AGENT_FEEDBACK V11（综合 2026-08-26~27：P16 修复轮 + 架构瘦身 + 卡帕西知识库——验证档位失实 / 产物持久化漏洞 / 子代理零产出 / 转义规则未硬编码）

## 一、总体判断

本反馈覆盖 08-26（P16 修复轮 + 架构瘦身）与 08-27（卡帕西知识库 v0.6 落地 + Q1-Q5
+ 优化 A-E）两个会话。**产出高价值**：五问题根因链全部取证锁定并修复、library 文件
夹化管理、P12 冻结+退役归档+work 清理 960MB、架构快照测试、卡帕西知识库（paperkb
包：L1-L3 编译/结果链增量/翻译迁移）、522+ 测试全绿。但暴露 **V10 未覆盖的五类
问题**：①**验证档位失实**——library 规范化声明"已实现"但 backend 会重新生成顶层
.en.md（只删文件没堵生成源，用户"你怎么验收的"）；②**产物持久化与生成流程耦合**
——assemble 的 mineru_full.md/source.pdf 被 backend 重建目录清掉；③**依赖链盘点
修正**——退役模块归档前未先出依赖图，靠逐模块 grep 才避免误删（P12/老 parse 链
认知修正）；④**子代理委派零产出**（08-27 paperparse 瘦身委派子代理卡断放弃，连续
9+ 会话子代理要么不用、要么失败）；⑤**V9/V10"pwsh -c 禁内联"硬规则仍未执行**。

**正向机制（值得保留）**：知识库方案先定稿讨论稿（v0.6，用户逐条拍板 D1-D20）再
实现——方案先行做对；架构快照测试（防误删生产依赖）；work 产物 5 版对比取证定位
回归；诚实认错+根因修复；小步提交保持（两天 16 commits）。

## 二、问题与根因（按用户要求 11 方面）

### 1. 编程习惯
- ✅ 取证先于结论（work 5 版产物对比、git 时间线、mineru 源/audit 多源交叉）；
  ✅ 小步提交（16 commits 主题清晰）；✅ 用户方向直接采纳（百度缩窄/引用上标/
  知识库 v0.6 逐条拍板后执行）；✅ 方案先行（KB-DESIGN v0.6 讨论稿在实现前）。
- ❌ **验证档位失实（最重，P5 违背）**：library 规范化验收只做"文件组装完成"检查，
  未做"生成方会不会覆盖/重建"的真实链路验证——用户 16:09 重新解析即复现顶层
  .en.md，质问"你怎么验收的"。教训：**结构/产物类改动，验收必须含生成方复查**。
- ❌ **只删产物不堵生成源**：删顶层 .en.md 但 out_dir bug 未修 → 必复发。教训：
  **删除产物必须同步定位并修复/禁用生成代码**。
- ❌ **产物自包含未预演"重跑覆盖"**：library 自包含（mineru_full/source.pdf）依赖
  "解析后 assemble"，backend 重建目录会清掉（P3 隐患预演缺失）。

### 2. 记忆系统
- ✅ 每轮后更新 SHORT-TERM、KBM 系列 NOTES 及时、交接清单完整（恢复指引明确）。
- ❌ 记忆 git 提交滞后（会话末一次性，未绑定 commit 节奏）；ARCH-REVIEW 落盘后
  实现中未同步更新；LESSONS 补充滞后（L022/L023 会话末才加）。

### 3. 思考强度
- 全程 high，执行轮（归档移动/work 清理/全量重跑/pytest/编译批量）未降档 low——
  执行轮思考 token 累积 O(N²)（V7-V10 已提，未改）。

### 4. 上下文管理
- ❌ 08-26 单会话横跨 5 子任务（分析→修复→library→C/D→架构瘦身）~200 轮，中间
  未交接（V7/V8"会话硬上限"机制 5 会话未生效）；08-27 分多子任务（Q5/KBM 系列）
  相对收敛但仍长。
- ❌ 大文件反复全文读：p14_pipeline 1700+ 行 ×5+、api.py 1000+ 行 ×4（应
  limit/offset 区间读）。

### 5. 上下文调用步数偏多（08-26 ~200 轮，原因）
- ①取证链长（顶层 .en.md 追查 ~25 轮——应先 grep 生成方再删文件）；②每改动全量
  验证（C/D 修复重跑 2 次、剥离后重跑 1 次——验证金字塔未执行）；③PowerShell
  Move-Item 到未存在目录把 html_elements.py 误改名 "core" 文件（3-4 轮返工——
  文件移动应 mkdir 后 move 或 fs 工具）；④pwsh -c 转义反复（见 8）；⑤架构依赖链
  逐模块 grep（10+ 次——应一次脚本出依赖图）。

### 6. token 主要消耗地方
- pwsh -c 内联 python 转义失败 6+ 次（每次 2-3 步）；大文件全文读；全量重跑 8+
  次（stats/conflicts 全量打印进历史）；目录树/文件清单未截断；长 edit。

### 7. 累积输入 token 后期暴增（O(N²)）
- ~200 轮 × 每轮 2-4 工具结果全量进前缀；用户逐条审阅（取证+修复+重跑+汇报）放大
  前缀租金——用户审得越细会话越贵（验收轮成本模型）；无自动压缩/交接触发。

### 8. 工具调用
- ❌ **pwsh -c 内联 python 转义（V9/V10 硬规则未执行）**：\\DeltaV 验证 3 次、
  _strip_math 2 次、_paper_dir 2 次——PowerShell 5.1 + Python + 正则三重转义不可
  预测。硬规则应升级：**除纯英文简单命令外一律 write work/*.py 再执行**。
- ❌ PowerShell Move-Item 到未存在目录 = 文件改名（html_elements.py→core）：
  文件系统操作优先 fs 工具。
- ❌ 大输出未落盘截断（survey 盘点被截断重跑）。

### 9. 子代理（连续 9+ 会话要么零使用要么失败）
- 08-26：0 使用（可委派：work 清理分类/依赖链盘点/僵尸测试审查/library 漏洞复现
  ——全部主线做了 ~40 轮）。
- 08-27：**paperparse 瘦身委派子代理零产出卡断放弃**（V10"硬超时/checkpoint/文件
  分区"教训仍未被硬编码执行）。**子代理已是连续 9 会话的最大负资产：不用浪费主线
  token，用了失败浪费更多**——必须创造模式硬编码"委派协议"（超时/落盘回报/文件
  边界），或默认主线做小任务、大任务拆最小可验证单元。

### 10. workflow 多模型分配（连续 9+ 会话 0 使用）
- 可用场景：①P14 生产链 import 依赖图扇出；②多篇 library 自包含完整性审计；
  ③百度缩窄策略批量用例回归；④L1-L3 编译质量抽检（便宜模型批量）。触发清单
  仍未硬编码。

### 11. 权限管理
- danger-full-access 全程无越权；先读后写防误覆盖 ✓；用户目录只读 ✓。
- ❌ 删除前清单核对不足：work 清理 54 目录删除前列了名字但未列文件数确认；
  archive 移动混乱（见 8）——**破坏性操作先列完整清单→用户可见→再执行**。

## 三、给创造模式的可执行条目（按 ROI，V11 新增/强化）

| 优先级 | 条目 | 预期收益 | 证据 |
|---|---|---|---|
| P0 | **"结构/产物类"改动验收 = 含生成方复查**：改目录/文件布局后验证"谁生成这些文件"且不会覆盖重建（真实链路非存在性） | 验收失实 0 | 顶层 .en.md 复发 |
| P0 | **删除/清理同步堵生成源**：删产物 = 同时禁用/修复生成代码（grep 生成方先于删除） | 清理复发 0 | 顶层 .en.md 只删不堵 |
| P0 | **子代理委派协议硬编码**（V10 教训仍未生效）：硬超时 + 每 N 步落盘回报 + 文件边界显式分配 + 零产出判卡死 | 委派失败 0 | 08-27 瘦身委派零产出 |
| P0 | **pwsh -c 禁用于含 $/反斜杠/正则/Unicode 命令**（V9/V10 升级为强执行） | 工具失败 -80% | 本会话 6+ 次 |
| P1 | **产物自包含预演"重跑覆盖"**：文件进目录自包含前先问"生成流程会不会重建该目录" | 方案漏洞 0 | library mineru_full 被清 |
| P1 | **文件移动/删除优先 fs 工具**（pwsh Move-Item 未存在目录 = 改名陷阱；先 mkdir） | 返工 -10 轮 | html_elements.py→core |
| P1 | **会话硬上限落地（V7-V10 P0 连续 5 会话未生效）**：>40 轮/3 子任务自动 pause+交接 | 前缀成本 -70% | 08-26 200 轮跨 5 子任务 |
| P1 | **依赖链盘点先于归档**：退役模块归档前一次脚本出"生产引用图" | 误删 0 | P12/老 parse 链认知修正 |
| P1 | 架构快照测试推广（test_architecture_snapshot.py 已实证防误删） | 防重蹈 | 已实证 |
| P2 | 破坏性操作先列完整清单+文件数再执行 | 误删风险 0 | work 清理 54 目录 |
| P2 | workflow 触发清单硬编码（依赖图/审计/用例回归/编译抽检强制扇出） | 主线轮次 -40% | 连续 9 会话 0 使用 |
| P2 | 大文件按区间读（limit/offset） | 前缀 -10% | p14 1700 行 ×5 |

## 四、关键数字（供对比 V10）

- 08-26：~180-220 轮；工具 ~220；主仓库 7 commits（d46440c/18a8639/88f8e8a/
  be35695/00dfb36/751f9c6/702e618）；子代理 0 / workflow 0；全量重跑 8+；pytest
  分段 8+（终 522 绿）；pwsh -c 转义失败 6+；Move-Item 返工 3-4 轮。
- 08-27：Q1-Q5 + 优化 A-E 交付（8+ commits：a1c777d/215a662/69b0609/7507277/
  e8f56c4/99e4dc2/292b1ab/2efb8d3 等）；卡帕西知识库 v0.6 落地（paperkb：bib
  导入/Roots/journals.db 22369 行/L1-L3 编译/结果链增量/翻译迁移/trigram 中文）；
  98+43/43+90/21+90 等测试全绿；CDP 实测通过；子代理 1（瘦身委派零产出放弃）。
- 用户批评/纠正（08-26）：①"library/<DOI>/ 根本没实现，你怎么验收的"；②顶层排版
  乱/空白行；③BF 下标 4（取证澄清=mineru+百度共识错误）；④"项目文件太多影响思考"。
- 新教训：**out_dir 硬编码层数 = 双场景必裂其一**（需自适应 _paper_dir）；
  **双通道只能修分歧不能修共识错误**（BF4- 需词典规则）；**"百度含公式/单位也准"
  一刀切放大百度错误**（权威范围=纯文本词级小噪声）；**架构快照测试**防误删；
  **删除/移动先 grep 生成方/先 mkdir**；**子代理零产出是最大负资产**（不用浪费
  主线、用了失败浪费更多——协议必须硬编码）。
'''

p = Path(r"D:\Python\DeepSeek\PaperAgent\docs\AGENT_FEEDBACK.md")
t = p.read_text(encoding="utf-8")
idx = t.find("# AGENT_FEEDBACK V11")
if idx == -1:
    raise SystemExit("V11 段未找到")
t = t[:idx].rstrip() + "\n" + V11
p.write_text(t, encoding="utf-8")
print("V11 综合版已重写，行数:", len(p.read_text(encoding="utf-8").splitlines()))
